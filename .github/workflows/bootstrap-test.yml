name: 'bootstrap-test'

description: 'tests the bootstrap script correctly installed and configured programs as required'

on:
  pull_request:
    branches: ['main']
  
jobs:
  docker:
    runs:
      using: 'docker'
      image: 'Dockerfile'
